## iteración 1
### prompt
necesito que implementes la tarea 2.3 del proyecto que es validar GeoMIP y QNodes contra fuerza bruta (bruteforce) en vez de pyphi, osea que el bruteforce sea el oraculo de referencia en vez de pyphi para esos test.

lo que ya existe: la clase `BruteForce` ya esta implementada en `code/QNodes/src/strategies/force.py` y en `code/GeoMIP/src/controllers/strategies/force.py`, ambas tienen el metodo `aplicar_estrategia` igual que las demas estrategias. lo que NO existe es el adaptador para el framework de tests ni los archivos de test.

lo que hay que hacer:

1. crear `code/tests/adapters/bruteforce_adapter.py` con dos clases, una para qnodes y otra para geomip (igual que como estan `QNodesPyPhiAdapter` y `GeoMIPPyPhiAdapter` en los adaptadores actuales) pero que en vez de llamar a `Phi` llamen a `BruteForce`. el adaptador debe implementar la interfaz `PyPhiAdapter` de `tests/adapters/base.py` para que el `BenchmarkRunner` lo acepte como referencia. ojo que el constructor de `BruteForce` en QNodes recibe el tpm directamente (`BruteForce(tpm)`) y en GeoMIP recibe un `Manager` (`BruteForce(Manager(...))`), osea que cada adaptador tiene que manejar eso segun su modulo.

2. crear `code/tests/test_geomip_vs_bruteforce.py` y `code/tests/test_qnodes_vs_bruteforce.py`, que sean iguales en estructura a `test_geomip_vs_pyphi.py` y `test_qnodes_vs_pyphi.py` (con la seccion `# CONFIGURE HERE` para cambiar ESTADO_INICIO, PAGINA, N_TESTS) pero con estas diferencias: la tolerancia `TOL_PHI` debe ser `1e-9` en vez de `1e-4`, el assert del test pytest debe exigir `100%` de phi_accuracy (no 60% ni 80%), y el `pyphi_adapter` del runner debe ser el nuevo adaptador de bruteforce no el de pyphi.

3. los reportes se deben generar igual que los test contra pyphi, osea csv y markdown en `code/tests/results/` usando el mismo `BenchmarkRunner` y `reporter` que ya existen, no hay que cambiar nada del runner ni del reporter.

4. esto aplica solo para n <= 6 (5 y 6 nodos) porque bruteforce es muy lento para n grandes, pero el codigo debe estar configurado igual de flexible que los tests de pyphi para poder cambiar el estado_inicio y que funcione.

no hay que tocar nada de los tests existentes contra pyphi ni del runner ni del reporter, solo agregar los archivos nuevos.

### respuesta
Se implementaron 3 archivos nuevos sin modificar nada existente:

1. `code/tests/adapters/bruteforce_adapter.py`: Dos clases `QNodesBruteForceAdapter` y `GeoMIPBruteForceAdapter`, ambas implementando `PyPhiAdapter`. `QNodesBruteForceAdapter` construye `BruteForce(tpm)` igual que `QNodesPyPhiAdapter` construía `Phi(tpm)`. `GeoMIPBruteForceAdapter` construye `BruteForce(Manager(estado_inicial=...))` igual que `GeoMIPPyPhiAdapter` construía `Phi(config)`. Ambas delegan a `aplicar_estrategia` con la misma firma que ya tienen.

2. `code/tests/test_geomip_vs_bruteforce.py`: Igual a `test_geomip_vs_pyphi.py` pero con `TOL_PHI = 1e-9`, `ESTADO_INICIO = "10000"` (5 nodos, n≤6), usa `GeoMIPBruteForceAdapter` como `pyphi_adapter`, y el assert exige `phi_accuracy_pct >= 100.0`.

3. `code/tests/test_qnodes_vs_bruteforce.py`: Igual a `test_qnodes_vs_pyphi.py` pero con `TOL_PHI = 1e-9`, `ESTADO_INICIO = "1" + "0" * 4` (5 nodos, n≤6), usa `QNodesBruteForceAdapter` como `pyphi_adapter`, y el assert exige `phi_accuracy_pct >= 100.0`.
