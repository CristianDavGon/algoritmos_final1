## iteración 1
### prompt
quiero que implementes las optimizaciones de GeometricSIA del plan que está en temp/PLAN_OPTIMIZACION_GEOMIP.md. Seguí el orden de ejecución que está al final del doc.

Primero hacé la Fase 1 completa: Opt-A (los dos print de ncube.py), Opt-B (_flat_data como matriz 2-D) y Opt-C (precomputar presentes). Después de cada una corré los tests para confirmar que no rompimos nada.

Para la Fase 2 (Opt-D y Opt-E) primero corrés code/tests/test_geomip_vs_pyphi.py para tener una línea base limpia, y recién después hacés el refactor coordinado. Esos dos van juntos en el mismo cambio porque son dependientes — no los separes. Al terminar comparás los resultados con la línea base para verificar que phi_accuracy_pct no cambió.

### respuesta
Se implementaron las 5 optimizaciones de GeometricSIA en dos fases:

**Fase 1 (Opt-A, Opt-B, Opt-C)** — aplicadas secuencialmente con tests verdes tras cada una:
- Opt-A: eliminados 2 `print` en `ncube.py:75,79` (dentro de `condicionar`).
- Opt-B: `_flat_data` (lista de arrays) reemplazado por `_flat_data_matrix` (numpy 2-D, shape `D_ncubos × 2^D`). Acceso en `calcular_costo` cambiado a indexación directa por columna.
- Opt-C: `presentes_comun` precomputado fuera del loop en `identificar_particiones_optimas`.

**Fase 2 (Opt-D + Opt-E)** — refactor coordinado en un solo cambio:
- Se añadió helper `_estado_a_int` (little-endian bit encoding).
- `tabla_transiciones` (dict con claves tupla) reemplazado por `self._tabla` (numpy array `2^D × D_ncubos`), con indexación directa `_tabla[fin_int]`.
- `self.caminos` ahora almacena enteros en lugar de listas. `calcular_costos_nivel` y `calcular_costo` operan sobre enteros con bit operations.
- Acumulación de predecesores vectorizada con numpy `+=`.
- `identificar_particiones_optimas` usa `_tabla[estado_int]` y bit masking para comparar estados.

**Comparación línea base vs post-refactor:**
- phi_accuracy_pct: 100.0% → 100.0% (sin cambio)
- Exactitud partición: 49/49 → 49/49
- Delta-phi máximo: 0.0 → 0.0
- Speedup promedio: 2.00x → 1.91x (variación normal de timing en N=5; ganancia real se verá en N≥10)
