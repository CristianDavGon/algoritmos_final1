## iteración 1
### prompt
Termina de implementar la fase 3, ademas crea la forma de probar kqnodes de forma facil como se hace para el qnodes, que sea correr un archivo donde en el archivo ya parametrice las k, los n, y me guarde los resultados en la carpeta de results. 

Terminar de implementar lo restante de la fase 3
Implementar una forma de correr (parametirzales k y n) para kqondes

### respuesta
Se completó la Fase 3 — KQNodes con estructura corregida:

**Runner (estructura igual que QNodes)**:
- `code/QNodes/exec_kqnodes.py` — punto de entrada idéntico a `exec.py`. Configura `N_VALORES`, `K_VALORES`, `CRITERIOS`, `MUESTRA` al tope.
- `code/QNodes/src/main_kqnodes.py` — lógica con `iniciar_kqnodes()`, análogo a `main.py`. Lee todas las filas del Excel, corre TODOS los k configurados por cada prueba. Guarda `results/kqnodes/resultados_N{n}{muestra}.csv` con columnas k y Criterio. Uso: `cd code/QNodes && python exec_kqnodes.py`.

**Bug corregido en `kqnodes.py`**: cuando N ≠ D (alcance y mecanismo de distinto tamaño), `_buscar_particion` usaba `V = frozenset(range(D))` generando índices d >= N que rompían `ncubos[d]` (tuple de tamaño N). Fix: `D_part = min(N, D)` + salvaguarda `if d >= N: continue` en `_calcular_phi_total`.

**mypy (tarea 3.13)**: `kqnodes.py` limpio. Corregidos: `slc_a`/`slc_b` → `list[slice | int]`, `aplicar_estrategia` → `# type: ignore[override]`.

**Tests**: 13/13 pasan. Corrida real: n=5, k=[2,3,4], C4 → 147 filas (49 pruebas × 3k) sin errores en `results/kqnodes/resultados_N5A.csv`.
