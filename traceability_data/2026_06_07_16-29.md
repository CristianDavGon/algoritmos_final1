## iteración 1
### prompt
La fase 3 está casi completada: KQNodes está implementado, solo faltan sus preubas.

Lo que quiero hacer ahora son tres cosas en orden.

**Primero**, reorganizar la estructura de carpetas de code/tests/ para que quede bien organizada. Actualmente hay adapters, runners, resultados y archivos de prueba mezclados. Quiero que la estructura sea clara y que además deje espacio para cuando llegue la fase 4 de KGeoMIP. Mira la estructura actual y propón una reorganización que tenga sentido, teniendo en cuenta que vamos a agregar pruebas para KQNodes (k=2,3,4,5 vs PyPhi/BruteForce) y más adelante pruebas para KGeoMIP. Aplica la reorganización sin romper nada de lo que ya funciona.

**Segundo**, estandarizar los archivos de ejecución. El objetivo no es crear documentos de texto, sino que los archivos exec.py y exec_kqnodes.py tengan un bloque de configuración estandarizado y claro al inicio, del estilo:

```python
# ── Configuración ─────────────────────────────────────────────────────────────
ESTADO:   str = "1" + "0" * 19
K:        int = 2
CRITERIO: str = "C4"   # "C4" = corte marginal mínimo, "C1" = tamaño máximo
MUESTRA:  str = "A"
# ─────────────────────────────────────────────────────────────────────────────
```

Además, crear runners individuales en code/tests/ para cada combinación algoritmo/comparación:
- `run_qnodes_vs_pyphi.py` — QNodes (k=2) contra PyPhi, todos los estados válidos
- `run_qnodes_vs_bruteforce.py` — QNodes (k=2) contra BruteForce, estados n≤8
- `run_kqnodes_vs_pyphi.py` — KQNodes (k=2,3,4,5) contra PyPhi, todos los estados válidos
- `run_kqnodes_vs_bruteforce.py` — KQNodes (k) contra BruteForce, estados n≤8 (placeholder: BruteForce k-partición aún no implementado)
- `run_geomip_vs_pyphi.py` — placeholder para fase 4
- `run_kgeomip_vs_pyphi.py` — placeholder para fase 4

Cada runner debe correr todos los estados válidos dentro de sus límites (PyPhi hasta n=10, BruteForce hasta n=8). Analizar hasta qué k es factible dado el costo combinatorio. Los runners usan el mismo framework de adapters de code/tests/adapters/.

**Tercero**, crear las pruebas de KQNodes contra PyPhi. PyPhi soporta k arbitrario: por defecto usa k=2 (biparticiones, `PARTITION_TYPE='BI'`), pero configurando `pyphi.config.PARTITION_TYPE = 'TRI'` usa k=3 y con `'ALL'` usa k sin restricción — todo vía la clase `KPartition` y `k_partitions()` de `pyphi/partition.py`. Por tanto PyPhi sirve como oráculo de referencia para k=2, k=3 y k≥4, aunque el costo combinatorio limita la factibilidad para k grande y n grande. Los tests para cada k deben configurar `PARTITION_TYPE` apropiadamente antes de invocar PyPhi. Para valores de k donde el costo sea prohibitivo (e.g. k=5, n≥7), los tests quedan como placeholders marcados como `pytest.skip` con una nota de límite de factibilidad, no por falta de oráculo. Analiza hasat cuando y con que n implementar esta pruebas

Tomar como referencia la estructura de test_qnodes_vs_pyphi.py para crear test_kqnodes_vs_pyphi.py, con:
- El mismo patrón de bloque de configuración en constantes (ESTADO, K, CRITERIO, MUESTRA)
- La misma función ejecutar_benchmark()
- La misma función test_*() para pytest
- Usar el adapter framework de code/tests/adapters/ y crear KQNodesAdapter si no existe
- Resultados en results/kqnodes/vs_pyphi/ con la misma convención de nombres

Ten cuidado con el problema de import shadowing documentado en los test files existentes: QNodes y GeoMIP comparten el mismo root package src.*, no se pueden correr juntos en el mismo proceso pytest.

### respuesta
Se implementaron los tres bloques en orden:

**Parte 1 — Reorganización de code/tests/:**
- Movidos test_geomip_vs_{pyphi,bruteforce}.py → tests/geomip/
- Movidos test_qnodes_vs_{pyphi,bruteforce}.py → tests/qnodes/
- Creados tests/kqnodes/ (nuevo) y tests/kgeomip/ (placeholder fase 4)
- Actualizado reporter.py: manejo de subdir "kqnodes" por prefijo y sanitización de nombres de archivo con caracteres especiales

**Parte 2 — Exec files y runners:**
- exec.py y exec_kqnodes.py ya tienen bloques de configuración estandarizados; se normalizó el espaciado del comentario en exec_kqnodes.py
- Creados 6 runners en code/tests/:
  - run_qnodes_vs_pyphi.py, run_qnodes_vs_bruteforce.py (funcionales)
  - run_kqnodes_vs_pyphi.py (funcional con análisis de factibilidad)
  - run_kqnodes_vs_bruteforce.py (placeholder: BruteForce k-partición pendiente)
  - run_geomip_vs_pyphi.py (funcional — GeoMIP ya implementado)
  - run_kgeomip_vs_pyphi.py (placeholder fase 4)

**Parte 3 — KQNodes vs PyPhi:**
- Creado tests/adapters/kqnodes_adapter.py con KQNodesAdapter y KQNodesPyPhiAdapter
  - KQNodesPyPhiAdapter configura pyphi.config.PARTITION_TYPE: k=2→'BI', k=3→'TRI', k≥4→'ALL'
- Creado tests/kqnodes/test_kqnodes_vs_pyphi.py con bloque ESTADO/K/CRITERIO/MUESTRA
- Análisis de factibilidad (redes disponibles: n=5, n=8, n=10):
  - k=2 ('BI'): n=5,8,10 ✓
  - k=3 ('TRI'): n=5,8 ✓ | n=10 skip (S(10,3)≈9330)
  - k=4 ('ALL'): n=5 ✓ | n=8,10 skip (B(8)=4140 particiones por llamada)
  - k=5 ('ALL'): n=5 ✓ | n≥7 skip (instrucción explícita)
- Límites implementados en _MAX_N_POR_K = {2:10, 3:8, 4:6, 5:6}
- reporter.py actualizado para mapear "kqnodes*" → results/kqnodes/

