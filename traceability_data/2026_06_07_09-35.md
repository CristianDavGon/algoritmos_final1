## iteración 1
### prompt
Desarrollo fase 3 kqnodes:
Desarrolla las tareas definidas en `context/state/active-tasks.md`, tomando primero como referencia todo el contenido de `context/`.

Para cada tarea, asegúrate de cumplir los criterios de Definition of Done (DoD) y de incluir pruebas correspondientes.

Al finalizar cada tarea, genera un informe en formato Markdown que incluya: qué se hizo, cómo se puede probar y qué queda pendiente de la fase 3.

### respuesta
Se implementaron las tareas 3.2–3.12 de la Fase 3 KQNodes:
- `code/QNodes/src/strategies/kqnodes.py`: clase `KQNodes` con criterio C4 (corte marginal mínimo), oracle restringido con remapeo de máscaras, caché por bloque, cálculo final de Φ* y variante C1.
- `code/QNodes/tests/test_kqnodes.py`: tests de regresión (k=2 ≡ QNodes), monotonicidad (φ(k+1) ≥ φ(k)), gap vs BruteForce, A/B C1 vs C4, generación de CSV de resultados.
Ver informe al final de la respuesta del agente.
